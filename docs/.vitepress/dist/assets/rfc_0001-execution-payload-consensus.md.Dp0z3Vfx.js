import{_ as a,c as t,o as i,ae as n}from"./chunks/framework.2MVDia4S.js";const u=JSON.parse('{"title":"RFC 0001: BFT Consensus over Ethereum Execution Payloads","description":"","frontmatter":{},"headers":[],"relativePath":"rfc/0001-execution-payload-consensus.md","filePath":"rfc/0001-execution-payload-consensus.md"}'),s={name:"rfc/0001-execution-payload-consensus.md"};function o(l,e,r,c,d,p){return i(),t("div",null,[...e[0]||(e[0]=[n(`<h1 id="rfc-0001-bft-consensus-over-ethereum-execution-payloads" tabindex="-1">RFC 0001: BFT Consensus over Ethereum Execution Payloads <a class="header-anchor" href="#rfc-0001-bft-consensus-over-ethereum-execution-payloads" aria-label="Permalink to &quot;RFC 0001: BFT Consensus over Ethereum Execution Payloads&quot;">​</a></h1><ul><li><strong>Status:</strong> Draft</li><li><strong>Category:</strong> Consensus</li><li><strong>Created:</strong> 2026-07-19</li><li><strong>Target release:</strong> EthBFT v0.1.0</li><li><strong>Supersedes:</strong> The asynchronous transaction-delivery protocol used through v0.0.10</li></ul><blockquote><p><strong>MVP implementation:</strong> v0.1.0 implements the Engine API V2 proposal, validation, execution commitment, and crash-recovery core of this RFC. The RFC remains Draft until multi-validator E2E, genesis-backed protocol parameters, transactional storage, and paired ABCI/EL state sync are complete.</p></blockquote><h2 id="abstract" tabindex="-1">Abstract <a class="header-anchor" href="#abstract" aria-label="Permalink to &quot;Abstract&quot;">​</a></h2><p>This RFC defines the first protocol in which EthBFT provides Byzantine fault tolerant consensus over Ethereum execution results.</p><p>CometBFT validators do not merely agree on a list of Ethereum transactions. They agree on a complete, versioned Ethereum execution payload. Every validator validates the proposed payload against an independent Ethereum Execution Layer (EL) client before accepting the CometBFT proposal. After CometBFT commits the proposal, every validator advances its local EL forkchoice to the committed payload and persists a commitment to the resulting Ethereum block and state.</p><p>The resulting safety property is:</p><blockquote><p>If fewer than one third of the CometBFT voting power is Byzantine, two honest EthBFT validators cannot commit different Ethereum execution blocks at the same EthBFT height.</p></blockquote><p>This protocol replaces the current asynchronous model in which CometBFT first commits transaction bytes and a single bridge process later attempts to deliver them to Geth.</p><h2 id="status-of-this-document" tabindex="-1">Status of This Document <a class="header-anchor" href="#status-of-this-document" aria-label="Permalink to &quot;Status of This Document&quot;">​</a></h2><p>This is a draft protocol specification. The keywords <strong>MUST</strong>, <strong>MUST NOT</strong>, <strong>REQUIRED</strong>, <strong>SHOULD</strong>, <strong>SHOULD NOT</strong>, and <strong>MAY</strong> are to be interpreted as normative requirements.</p><p>The protocol is not backward-compatible with the v0.0.10 application state or chain history. Implementations of this RFC MUST start a new CometBFT chain and use a new EthBFT protocol version.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>The v0.0.10 design has three fundamental limitations:</p><ol><li>CometBFT commits transactions before Ethereum execution succeeds.</li><li>A single bridge process decides which Geth payload becomes canonical.</li><li>The ABCI application hash does not commit to the Ethereum block hash, state root, receipts root, or transaction root.</li></ol><p>Consequently, CometBFT consensus currently provides transaction ordering but does not provide consensus over Ethereum state. That design is useful as a delivery demo but is not an EthBFT consensus protocol.</p><p>This RFC makes the Ethereum execution payload itself part of the CometBFT proposal and requires every validator to validate the same state transition.</p><h2 id="goals" tabindex="-1">Goals <a class="header-anchor" href="#goals" aria-label="Permalink to &quot;Goals&quot;">​</a></h2><p>The protocol MUST provide:</p><ul><li>BFT agreement on one Ethereum block hash per CometBFT height.</li><li>Independent execution validation by every validator&#39;s EL client.</li><li>Exact agreement on transaction membership and order.</li><li>A deterministic ABCI application hash that commits to Ethereum execution.</li><li>Crash-safe, idempotent replay of decided CometBFT blocks.</li><li>Deterministic Engine API version selection across Ethereum forks.</li><li>Rejection of proposals that cannot be fully validated locally.</li><li>A path for new validators to obtain a matching CometBFT and EL checkpoint.</li></ul><h2 id="non-goals" tabindex="-1">Non-goals <a class="header-anchor" href="#non-goals" aria-label="Permalink to &quot;Non-goals&quot;">​</a></h2><p>This RFC does not define:</p><ul><li>A trustless bridge between EthBFT and Ethereum mainnet.</li><li>An Ethereum consensus-layer implementation or beacon-chain compatibility.</li><li>MEV policy, proposer-builder separation, or encrypted mempools.</li><li>Validator selection or staking rules beyond those supplied by CometBFT.</li><li>Fraud proofs or validity proofs for clients that do not run an EL.</li><li>Cross-chain asset custody or message-passing contracts.</li></ul><p>These features may be specified by later RFCs.</p><h2 id="terminology" tabindex="-1">Terminology <a class="header-anchor" href="#terminology" aria-label="Permalink to &quot;Terminology&quot;">​</a></h2><ul><li><strong>Consensus height:</strong> A CometBFT block height.</li><li><strong>Execution block:</strong> The Ethereum block committed at one consensus height.</li><li><strong>Execution parent:</strong> The execution block committed at the previous consensus height.</li><li><strong>Candidate transaction:</strong> A signed Ethereum transaction available to the proposer when building a payload.</li><li><strong>Execution metadata:</strong> Consensus data needed to reconstruct and validate an Engine API request, excluding the transaction byte arrays stored separately in the CometBFT proposal.</li><li><strong>Execution commitment:</strong> A deterministic commitment to the committed execution block and its state transition.</li><li><strong>Local EL:</strong> The dedicated Ethereum execution client operated by one EthBFT validator.</li><li><strong>Protocol parameters:</strong> Consensus-critical EthBFT configuration included in genesis or updated through a future governance mechanism.</li></ul><h2 id="system-model" tabindex="-1">System Model <a class="header-anchor" href="#system-model" aria-label="Permalink to &quot;System Model&quot;">​</a></h2><p>Each CometBFT validator MUST operate:</p><ol><li>one EthBFT ABCI application instance; and</li><li>one independent, dedicated Ethereum EL instance exposing the authenticated Engine API.</li></ol><p>An EL instance MUST NOT be shared by validators that are intended to fail independently. Public JSON-RPC access MUST NOT be allowed to mutate the EL chain, txpool, or forkchoice outside the EthBFT process.</p><p>All validators MUST use the same:</p><ul><li>Ethereum genesis block and chain ID;</li><li>Ethereum fork schedule;</li><li>EthBFT protocol parameters;</li><li>Engine API method selection rules; and</li><li>execution block gas limit policy.</li></ul><p>A validator that cannot validate a proposal because its EL is offline, syncing, on the wrong chain, or running an incompatible fork MUST reject the proposal and report itself not ready. It MUST NOT vote based only on the proposer&#39;s claimed roots.</p><h2 id="protocol-overview" tabindex="-1">Protocol Overview <a class="header-anchor" href="#protocol-overview" aria-label="Permalink to &quot;Protocol Overview&quot;">​</a></h2><p>For consensus height <code>H</code>:</p><ol><li>The proposer builds an Ethereum execution payload whose parent is the execution block committed at height <code>H - 1</code>.</li><li>The proposer places one <code>ExecutionMetadata</code> item followed by the exact raw Ethereum transactions from the payload into the CometBFT proposal.</li><li>Each validator reconstructs the complete Engine API payload from the metadata and ordered transactions.</li><li>Each validator checks all consensus rules and calls <code>engine_newPayloadVx</code> against its local EL.</li><li>A validator accepts the proposal only when the EL returns <code>VALID</code> and the validated block hash equals the proposed block hash.</li><li>CometBFT reaches consensus on the proposal.</li><li>During finalization, each validator advances local EL forkchoice to the committed block and persists the new execution commitment.</li></ol><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sequenceDiagram</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    participant C as Candidate transactions</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    participant P as CometBFT proposer</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    participant PE as Proposer EL</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    participant V as CometBFT validators</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    participant VE as Validator ELs</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    C-&gt;&gt;P: signed Ethereum transactions</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    P-&gt;&gt;PE: forkchoiceUpdated + getPayload</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    PE--&gt;&gt;P: ExecutionPayload</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    P-&gt;&gt;V: metadata + exact ordered payload transactions</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    V-&gt;&gt;VE: engine_newPayloadVx</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    VE--&gt;&gt;V: VALID + block hash</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    V-&gt;&gt;V: CometBFT prevote/precommit</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    V-&gt;&gt;VE: forkchoiceUpdated(committed head)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    V-&gt;&gt;V: persist execution commitment</span></span></code></pre></div><h2 id="consensus-proposal-format" tabindex="-1">Consensus Proposal Format <a class="header-anchor" href="#consensus-proposal-format" aria-label="Permalink to &quot;Consensus Proposal Format&quot;">​</a></h2><h3 id="proposal-transaction-layout" tabindex="-1">Proposal transaction layout <a class="header-anchor" href="#proposal-transaction-layout" aria-label="Permalink to &quot;Proposal transaction layout&quot;">​</a></h3><p>At height <code>H</code>, <code>RequestPrepareProposal.Txs</code> MUST be transformed into:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Txs[0]   = ETHBFT_ENVELOPE_PREFIX || encoded ExecutionMetadata</span></span>
<span class="line"><span>Txs[1]   = first raw Ethereum transaction in ExecutionPayload.transactions</span></span>
<span class="line"><span>Txs[2]   = second raw Ethereum transaction in ExecutionPayload.transactions</span></span>
<span class="line"><span>...</span></span>
<span class="line"><span>Txs[N]   = final raw Ethereum transaction in ExecutionPayload.transactions</span></span></code></pre></div><p><code>ETHBFT_ENVELOPE_PREFIX</code> is the eight-byte ASCII value <code>ETHBFT\\x00\\x01</code>. Ordinary Ethereum transactions cannot use this prefix as a valid typed or legacy transaction encoding.</p><p>There MUST be exactly one envelope and it MUST be the first proposal item. Every remaining proposal item MUST be a valid raw Ethereum transaction.</p><p>The transaction list reconstructed from <code>Txs[1:N]</code> MUST exactly equal <code>ExecutionPayload.transactions</code>, including byte representation, membership, and order. The metadata MUST NOT contain a second copy of transaction bytes.</p><p>The entire proposal MUST fit within CometBFT&#39;s <code>MaxTxBytes</code>. A proposer MUST NOT truncate an already-built payload. It must request or construct another payload that fits, or propose an empty execution block.</p><h3 id="encoding" tabindex="-1">Encoding <a class="header-anchor" href="#encoding" aria-label="Permalink to &quot;Encoding&quot;">​</a></h3><p><code>ExecutionMetadata</code> MUST use the versioned canonical RLP codec defined in the implementation package <code>pkg/protocol</code>. It is encoded as one RLP list whose field order is the order in <code>ExecutionMetadata</code> below. Integers use RLP&#39;s minimal unsigned big-endian representation; fixed hashes and addresses use their raw byte representation. Unknown versions, non-minimal integer encodings, trailing bytes, unknown fields, and list-length mismatches MUST be rejected. JSON MUST NOT be used as the consensus encoding.</p><p>Consensus hashes are computed from explicitly defined fields and MUST NOT rely on map iteration order or implementation-specific object serialization.</p><h3 id="executionmetadata" tabindex="-1">ExecutionMetadata <a class="header-anchor" href="#executionmetadata" aria-label="Permalink to &quot;ExecutionMetadata&quot;">​</a></h3><p>The following field order is normative. Protocol v1 omits the final four post-Cancun fields from its RLP list and rejects payloads that require them.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ExecutionMetadata {</span></span>
<span class="line"><span>    protocol_version:          uint32</span></span>
<span class="line"><span>    engine_api_version:        uint8</span></span>
<span class="line"><span>    chain_id:                  uint256</span></span>
<span class="line"><span>    consensus_height:          uint64</span></span>
<span class="line"><span>    previous_app_hash:         bytes32</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    parent_hash:               bytes32</span></span>
<span class="line"><span>    fee_recipient:             bytes20</span></span>
<span class="line"><span>    state_root:                bytes32</span></span>
<span class="line"><span>    receipts_root:             bytes32</span></span>
<span class="line"><span>    logs_bloom:                bytes256</span></span>
<span class="line"><span>    prev_randao:               bytes32</span></span>
<span class="line"><span>    block_number:              uint64</span></span>
<span class="line"><span>    gas_limit:                 uint64</span></span>
<span class="line"><span>    gas_used:                  uint64</span></span>
<span class="line"><span>    timestamp:                 uint64</span></span>
<span class="line"><span>    extra_data:                bytes</span></span>
<span class="line"><span>    base_fee_per_gas:          uint256</span></span>
<span class="line"><span>    block_hash:                bytes32</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    withdrawals:               list&lt;Withdrawal&gt;</span></span>
<span class="line"><span>    blob_gas_used:             optional&lt;uint64&gt;</span></span>
<span class="line"><span>    excess_blob_gas:           optional&lt;uint64&gt;</span></span>
<span class="line"><span>    versioned_hashes:          list&lt;bytes32&gt;</span></span>
<span class="line"><span>    parent_beacon_block_root:  optional&lt;bytes32&gt;</span></span>
<span class="line"><span>    execution_requests:        list&lt;bytes&gt;</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>The execution payload&#39;s transactions are taken from the proposal items after the envelope. <code>transactions_root</code> is recomputed from those transactions and is not trusted from metadata.</p><p>The metadata fields present for a given height MUST exactly match the Engine API version selected by the protocol fork schedule.</p><h2 id="consensus-critical-parameters" tabindex="-1">Consensus-Critical Parameters <a class="header-anchor" href="#consensus-critical-parameters" aria-label="Permalink to &quot;Consensus-Critical Parameters&quot;">​</a></h2><p>The following values MUST be stored in EthBFT genesis state and included in the genesis application hash:</p><ul><li><code>protocol_version</code></li><li>Ethereum <code>chain_id</code></li><li>Ethereum genesis block hash</li><li>Ethereum fork activation schedule</li><li>maximum execution gas limit</li><li>maximum transaction bytes per proposal</li><li>fee recipient policy</li><li>withdrawal policy</li><li>deterministic <code>prev_randao</code> derivation rule</li><li>deterministic parent beacon block root derivation rule</li><li>maximum <code>extra_data</code> length</li><li>supported Engine API versions</li></ul><p>Local YAML configuration MAY specify endpoints, JWT paths, logging, and timeouts. It MUST NOT override consensus-critical parameters.</p><p>The application MUST fail startup when local EL chain ID, genesis hash, or fork schedule does not match genesis state.</p><h2 id="deterministic-header-inputs" tabindex="-1">Deterministic Header Inputs <a class="header-anchor" href="#deterministic-header-inputs" aria-label="Permalink to &quot;Deterministic Header Inputs&quot;">​</a></h2><h3 id="parent-hash" tabindex="-1">Parent hash <a class="header-anchor" href="#parent-hash" aria-label="Permalink to &quot;Parent hash&quot;">​</a></h3><p>For height <code>H &gt; initial_height</code>, <code>parent_hash</code> MUST equal the execution block hash committed at <code>H - 1</code>.</p><p>At <code>initial_height</code>, <code>parent_hash</code> MUST equal the execution genesis block hash defined in EthBFT genesis state.</p><h3 id="timestamp" tabindex="-1">Timestamp <a class="header-anchor" href="#timestamp" aria-label="Permalink to &quot;Timestamp&quot;">​</a></h3><p>The execution payload timestamp MUST equal the Unix timestamp from the CometBFT proposed block header.</p><p>It MUST be strictly greater than the execution parent&#39;s timestamp. Validators MUST reject a proposal that violates either condition. EthBFT MUST NOT replace the consensus timestamp with a validator&#39;s local wall clock.</p><h3 id="prevrandao" tabindex="-1">prevRandao <a class="header-anchor" href="#prevrandao" aria-label="Permalink to &quot;prevRandao&quot;">​</a></h3><p>For protocol version 1:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>prev_randao = keccak256(</span></span>
<span class="line"><span>    &quot;ETHBFT_PREVRANDAO_V1&quot; ||</span></span>
<span class="line"><span>    uint256_be(chain_id) ||</span></span>
<span class="line"><span>    uint64_be(consensus_height) ||</span></span>
<span class="line"><span>    previous_app_hash</span></span>
<span class="line"><span>)</span></span></code></pre></div><p>This value is deterministic and is not intended to provide unbiasable randomness. Applications requiring secure randomness MUST use a separately specified randomness protocol.</p><h3 id="fee-recipient" tabindex="-1">Fee recipient <a class="header-anchor" href="#fee-recipient" aria-label="Permalink to &quot;Fee recipient&quot;">​</a></h3><p>The fee recipient MUST be derived from the consensus fee recipient policy. The initial policy is a single address fixed in genesis. A local validator setting MUST NOT change it.</p><h3 id="withdrawals-and-beacon-root" tabindex="-1">Withdrawals and beacon root <a class="header-anchor" href="#withdrawals-and-beacon-root" aria-label="Permalink to &quot;Withdrawals and beacon root&quot;">​</a></h3><p>Protocol version 1 uses an empty withdrawals list unless genesis defines a deterministic withdrawal source.</p><p>When the active Engine API version requires <code>parent_beacon_block_root</code>, it is:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>parent_beacon_block_root = keccak256(</span></span>
<span class="line"><span>    &quot;ETHBFT_BEACON_ROOT_V1&quot; ||</span></span>
<span class="line"><span>    uint64_be(consensus_height) ||</span></span>
<span class="line"><span>    previous_app_hash</span></span>
<span class="line"><span>)</span></span></code></pre></div><p>This field exists to satisfy Ethereum execution semantics; EthBFT does not claim that it represents an Ethereum beacon block.</p><h2 id="engine-api-version-selection" tabindex="-1">Engine API Version Selection <a class="header-anchor" href="#engine-api-version-selection" aria-label="Permalink to &quot;Engine API Version Selection&quot;">​</a></h2><p>Engine API version selection MUST be a pure function of the consensus timestamp and the fork schedule in genesis:</p><table tabindex="0"><thead><tr><th>Active execution fork</th><th>forkchoiceUpdated</th><th>getPayload</th><th>newPayload</th></tr></thead><tbody><tr><td>Shanghai</td><td>V2</td><td>V2</td><td>V2</td></tr><tr><td>Cancun</td><td>V3</td><td>V3</td><td>V3</td></tr><tr><td>Prague or later with execution requests</td><td>V3</td><td>V4</td><td>V4</td></tr></tbody></table><p>The exact method matrix MUST track the execution specification adopted by the chain. Changing it requires a protocol upgrade activated at a consensus height or timestamp.</p><p>Validators MUST reject envelopes whose declared method version differs from the deterministic version for that height.</p><h2 id="abci-method-semantics" tabindex="-1">ABCI Method Semantics <a class="header-anchor" href="#abci-method-semantics" aria-label="Permalink to &quot;ABCI Method Semantics&quot;">​</a></h2><h3 id="info" tabindex="-1">Info <a class="header-anchor" href="#info" aria-label="Permalink to &quot;Info&quot;">​</a></h3><p><code>Info</code> MUST return the last fully committed consensus height and its execution commitment as <code>LastBlockAppHash</code>.</p><p>It MUST NOT report a height that exists only in a prepared write-ahead record.</p><h3 id="checktx" tabindex="-1">CheckTx <a class="header-anchor" href="#checktx" aria-label="Permalink to &quot;CheckTx&quot;">​</a></h3><p><code>CheckTx</code> performs admission checks only. It MUST validate:</p><ul><li>maximum transaction size;</li><li>Ethereum transaction decoding;</li><li>chain ID;</li><li>signature;</li><li>transaction type allowed by the active fork; and</li><li>reserved envelope-prefix exclusion.</li></ul><p><code>CheckTx</code> success does not promise inclusion or execution. This is normal mempool admission semantics, not the asynchronous delivery status used by the old protocol.</p><h3 id="prepareproposal" tabindex="-1">PrepareProposal <a class="header-anchor" href="#prepareproposal" aria-label="Permalink to &quot;PrepareProposal&quot;">​</a></h3><p>Only the current proposer builds a payload.</p><p>The proposer MUST:</p><ol><li>read the committed execution parent from durable state;</li><li>derive deterministic payload attributes from the request and protocol parameters;</li><li>make candidate Ethereum transactions available to its dedicated builder;</li><li>call the version-selected <code>engine_forkchoiceUpdatedVx</code> with payload attributes;</li><li>call <code>engine_getPayloadVx</code>;</li><li>reject the local build result unless its parent and deterministic attributes match the required values;</li><li>construct <code>ExecutionMetadata</code> from the payload;</li><li>return the envelope followed by exactly the payload transactions in payload order; and</li><li>ensure the proposal fits the CometBFT byte limit.</li></ol><p>The proposer MAY select, omit, and reorder candidate mempool transactions as allowed by Ethereum validity rules. The resulting payload transaction order, not the arrival order in the CometBFT mempool, is the consensus order.</p><p>An implementation MAY use a dedicated Geth payload builder initially. A future builder interface may provide explicit candidate allowlists. Regardless of the builder implementation, validators trust only the payload they independently validate.</p><p><code>PrepareProposal</code> MUST NOT advance canonical EL forkchoice to the candidate payload.</p><h3 id="processproposal" tabindex="-1">ProcessProposal <a class="header-anchor" href="#processproposal" aria-label="Permalink to &quot;ProcessProposal&quot;">​</a></h3><p>Every validator MUST perform all of the following checks before returning <code>ACCEPT</code>:</p><ol><li>Decode exactly one envelope at proposal index zero.</li><li>Reject unknown protocol or Engine API versions.</li><li>Verify chain ID, consensus height, and previous app hash.</li><li>Verify parent hash equals the locally committed execution head.</li><li>Verify all deterministic header inputs and consensus parameters.</li><li>Decode every remaining item as an Ethereum transaction.</li><li>Reject duplicate transaction hashes within the payload.</li><li>Reconstruct the complete versioned Engine API payload.</li><li>Recompute and compare the transaction root and execution block hash.</li><li>Call the version-selected <code>engine_newPayloadVx</code> on the local EL.</li><li>Require payload status <code>VALID</code>.</li><li>Require <code>latestValidHash</code> to equal the proposed block hash when the EL returns a non-null latest valid hash.</li></ol><p><code>ACCEPTED</code>, <code>SYNCING</code>, a timeout, an unavailable EL, a null response, or any other status MUST cause <code>REJECT</code>.</p><p><code>ProcessProposal</code> may insert a validated non-canonical block into the local EL database. It MUST NOT update canonical forkchoice.</p><p>Validation results MAY be cached by <code>(consensus_height, block_hash)</code>. Cache entries are non-authoritative and MUST be safe to discard.</p><h3 id="finalizeblock" tabindex="-1">FinalizeBlock <a class="header-anchor" href="#finalizeblock" aria-label="Permalink to &quot;FinalizeBlock&quot;">​</a></h3><p><code>FinalizeBlock</code> is called for the block decided by CometBFT. The application MUST:</p><ol><li>repeat all deterministic checks;</li><li>require a successful local EL validation, re-running <code>engine_newPayloadVx</code> if no valid cache entry exists;</li><li>compute the new execution commitment;</li><li>durably write a prepared commit record;</li><li>call <code>engine_forkchoiceUpdatedVx</code> with the decided execution block; and</li><li>stage the new application height and hash for <code>Commit</code>.</li></ol><p>All Ethereum transaction results returned by <code>FinalizeBlock</code> represent transactions included in a valid, decided execution payload. There is no later asynchronous delivery phase.</p><h3 id="commit" tabindex="-1">Commit <a class="header-anchor" href="#commit" aria-label="Permalink to &quot;Commit&quot;">​</a></h3><p><code>Commit</code> MUST atomically mark the prepared record committed and fsync the application state before returning success.</p><p>After <code>Commit</code>, <code>Info</code> MUST report the new height and app hash.</p><h3 id="query" tabindex="-1">Query <a class="header-anchor" href="#query" aria-label="Permalink to &quot;Query&quot;">​</a></h3><p>The application SHOULD expose deterministic queries for:</p><ul><li>consensus height to execution block hash;</li><li>execution block hash to consensus height;</li><li>transaction hash to committed execution block;</li><li>current execution commitment; and</li><li>protocol parameters and active fork version.</li></ul><p>Query results intended for remote verification SHOULD include CometBFT query proofs where supported.</p><h3 id="snapshot-methods" tabindex="-1">Snapshot methods <a class="header-anchor" href="#snapshot-methods" aria-label="Permalink to &quot;Snapshot methods&quot;">​</a></h3><p>Production deployments MUST implement ABCI state snapshots. A snapshot MUST identify a matching EL checkpoint by block hash and state root. Restoring only the ABCI database without restoring or synchronizing the matching EL state is invalid.</p><p>The first development milestone MAY require validators to replay from genesis, but the network MUST NOT be described as production-ready until checkpointed state sync is implemented and tested.</p><h2 id="execution-commitment-and-app-hash" tabindex="-1">Execution Commitment and App Hash <a class="header-anchor" href="#execution-commitment-and-app-hash" aria-label="Permalink to &quot;Execution Commitment and App Hash&quot;">​</a></h2><p>For protocol version 1, define:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>execution_commitment = keccak256(</span></span>
<span class="line"><span>    &quot;ETHBFT_EXECUTION_COMMITMENT_V1&quot; ||</span></span>
<span class="line"><span>    uint256_be(chain_id) ||</span></span>
<span class="line"><span>    uint64_be(consensus_height) ||</span></span>
<span class="line"><span>    parent_hash ||</span></span>
<span class="line"><span>    block_hash ||</span></span>
<span class="line"><span>    state_root ||</span></span>
<span class="line"><span>    receipts_root ||</span></span>
<span class="line"><span>    transactions_root</span></span>
<span class="line"><span>)</span></span></code></pre></div><p>The ABCI app hash is:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>app_hash = sha256(</span></span>
<span class="line"><span>    &quot;ETHBFT_APP_HASH_V1&quot; ||</span></span>
<span class="line"><span>    previous_app_hash ||</span></span>
<span class="line"><span>    execution_commitment ||</span></span>
<span class="line"><span>    protocol_parameters_hash</span></span>
<span class="line"><span>)</span></span></code></pre></div><p>All fixed-width integers use unsigned big-endian encoding. Hash fields are raw 32-byte values without textual <code>0x</code> prefixes.</p><p><code>protocol_parameters_hash</code> is a deterministic commitment to the active consensus-critical parameters. Its precise canonical container MUST be defined before this RFC reaches Accepted status.</p><p>The app hash commits to Ethereum execution state; it is not itself an Ethereum state root.</p><h2 id="el-forkchoice-and-finality" tabindex="-1">EL Forkchoice and Finality <a class="header-anchor" href="#el-forkchoice-and-finality" aria-label="Permalink to &quot;EL Forkchoice and Finality&quot;">​</a></h2><p>Once a CometBFT block is decided, its execution payload is BFT-final under the CometBFT fault assumption. Protocol version 1 therefore sets:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>headBlockHash      = committed block hash</span></span>
<span class="line"><span>safeBlockHash      = committed block hash</span></span>
<span class="line"><span>finalizedBlockHash = committed block hash</span></span></code></pre></div><p>Validators MUST NOT configure local safe or finalized depth values. Any future delayed-finality policy must be consensus-critical and introduced through a protocol upgrade.</p><p>The local EL MUST NOT be allowed to select another canonical head. EthBFT is the only forkchoice authority for its dedicated EL.</p><h2 id="durable-state" tabindex="-1">Durable State <a class="header-anchor" href="#durable-state" aria-label="Permalink to &quot;Durable State&quot;">​</a></h2><p>The JSON state file used through v0.0.10 is insufficient for this protocol. Implementations MUST use a transactional state store with:</p><ul><li>atomic batches;</li><li>write-ahead recovery;</li><li>fsync before acknowledging <code>Commit</code>;</li><li>corruption detection;</li><li>schema versioning;</li><li>exclusive process locking; and</li><li>checkpoint or backup support.</li></ul><p>The minimum committed record contains:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CommittedExecutionState {</span></span>
<span class="line"><span>    schema_version</span></span>
<span class="line"><span>    protocol_version</span></span>
<span class="line"><span>    chain_id</span></span>
<span class="line"><span>    ethereum_genesis_hash</span></span>
<span class="line"><span>    consensus_height</span></span>
<span class="line"><span>    execution_block_number</span></span>
<span class="line"><span>    execution_block_hash</span></span>
<span class="line"><span>    execution_parent_hash</span></span>
<span class="line"><span>    state_root</span></span>
<span class="line"><span>    receipts_root</span></span>
<span class="line"><span>    transactions_root</span></span>
<span class="line"><span>    app_hash</span></span>
<span class="line"><span>    protocol_parameters_hash</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>The store MUST retain enough recent height mappings to serve proofs and detect replay conflicts. Historical pruning rules require a separate specification.</p><h2 id="crash-recovery-and-replay" tabindex="-1">Crash Recovery and Replay <a class="header-anchor" href="#crash-recovery-and-replay" aria-label="Permalink to &quot;Crash Recovery and Replay&quot;">​</a></h2><p>All ABCI handlers MUST be deterministic and idempotent for the same decided block.</p><p>On startup, EthBFT MUST:</p><ol><li>acquire exclusive ownership of its state store;</li><li>load the last committed application record;</li><li>verify local EL chain ID and genesis hash;</li><li>query the committed execution block by hash;</li><li>verify its number, parent, state root, and canonical status;</li><li>inspect any prepared but uncommitted record; and</li><li>reconcile local EL forkchoice with the last committed record.</li></ol><p>Recovery rules:</p><ul><li>If the committed execution block is present but non-canonical, EthBFT MUST restore forkchoice to it before becoming ready.</li><li>If a prepared block is present in the EL but application <code>Commit</code> was not completed, EthBFT MUST report only the prior committed height and allow CometBFT to replay the decided block.</li><li>Replaying the same <code>(height, block_hash)</code> MUST succeed idempotently.</li><li>Observing a different block hash for an already committed height is a fatal safety violation. The node MUST halt.</li><li>If the committed block or required EL state is unavailable, the validator MUST remain unready and MUST NOT participate in consensus until state is restored.</li></ul><h2 id="validator-readiness" tabindex="-1">Validator Readiness <a class="header-anchor" href="#validator-readiness" aria-label="Permalink to &quot;Validator Readiness&quot;">​</a></h2><p>A validator is ready to vote only when:</p><ul><li>the ABCI state store is healthy;</li><li>the local EL is reachable and not syncing;</li><li>chain ID, genesis hash, and fork schedule match;</li><li>local EL canonical head equals the committed execution head;</li><li>no unresolved state-store reconciliation exists; and</li><li>the node supports the Engine API version active at the next height.</li></ul><p>Liveness and readiness endpoints MUST be separate. Liveness indicates that the process can serve requests. Readiness indicates that it is safe to participate in consensus.</p><p>CometBFT validator operation SHOULD be automatically disabled or externally alerted when EthBFT readiness fails.</p><h2 id="byzantine-and-failure-cases" tabindex="-1">Byzantine and Failure Cases <a class="header-anchor" href="#byzantine-and-failure-cases" aria-label="Permalink to &quot;Byzantine and Failure Cases&quot;">​</a></h2><p>Validators MUST reject at least the following proposal faults:</p><ul><li>wrong execution parent;</li><li>wrong consensus height or previous app hash;</li><li>invalid or unsupported envelope encoding;</li><li>envelope not at proposal index zero;</li><li>wrong Engine API version;</li><li>local-clock-derived timestamp;</li><li>wrong <code>prev_randao</code>, fee recipient, withdrawals, or beacon root;</li><li>malformed, unsigned, wrong-chain, or duplicate transactions;</li><li>mismatch between proposal transaction order and execution payload;</li><li>invalid block hash, transaction root, state root, or receipts root;</li><li>invalid blob versioned hashes or execution requests;</li><li>EL response other than <code>VALID</code>;</li><li>payload exceeding CometBFT or Ethereum limits; and</li><li>proposal for a height that conflicts with committed state.</li></ul><p>A Byzantine proposer can choose transaction membership and ordering, subject to Ethereum validity and CometBFT limits. This affects fairness and censorship but does not violate execution safety. Fair-ordering mechanisms are outside this RFC.</p><h2 id="data-availability" tabindex="-1">Data Availability <a class="header-anchor" href="#data-availability" aria-label="Permalink to &quot;Data Availability&quot;">​</a></h2><p>The full execution metadata and transaction bytes are included in the CometBFT block. A validator therefore has all block-body data needed for execution validation.</p><p>For blob transactions, the Engine API payload contains versioned hashes but not necessarily all blob sidecar data. Before Cancun support is enabled, a follow-up specification MUST define blob sidecar propagation, availability validation, retention, and recovery. Until then, EthBFT genesis MUST keep blob transactions disabled.</p><h2 id="networking-and-security" tabindex="-1">Networking and Security <a class="header-anchor" href="#networking-and-security" aria-label="Permalink to &quot;Networking and Security&quot;">​</a></h2><ul><li>Engine API endpoints MUST remain private and JWT-authenticated.</li><li>Public JSON-RPC transaction submission to validator ELs SHOULD be disabled.</li><li>Ethereum transactions SHOULD enter through the CometBFT RPC or a controlled EthBFT admission endpoint.</li><li>Consensus and P2P ports MUST follow standard CometBFT security guidance.</li><li>Query, health, readiness, and metrics endpoints MUST have explicit exposure policies.</li><li>Protocol logs MUST include consensus height, execution block hash, parent hash, Engine API version, validation result, and commit phase.</li><li>Secrets MUST never be logged.</li></ul><h2 id="observability-requirements" tabindex="-1">Observability Requirements <a class="header-anchor" href="#observability-requirements" aria-label="Permalink to &quot;Observability Requirements&quot;">​</a></h2><p>Implementations MUST expose at least:</p><ul><li>committed consensus height;</li><li>committed EL block number and hash;</li><li>proposal build duration;</li><li>proposal validation duration;</li><li>Engine API status counts by method and response status;</li><li>proposal rejection counts by stable reason code;</li><li>EL/ABCI head mismatch;</li><li>state-store commit and fsync failures;</li><li>replay and reconciliation counts; and</li><li>validator readiness state.</li></ul><p>Alerts SHOULD cover stalled consensus, EL divergence, repeated proposal rejection, unsupported upcoming fork, state corruption, and readiness failure.</p><h2 id="compatibility-and-migration" tabindex="-1">Compatibility and Migration <a class="header-anchor" href="#compatibility-and-migration" aria-label="Permalink to &quot;Compatibility and Migration&quot;">​</a></h2><p>There is no safe in-place migration from the v0.0.10 chain because historical CometBFT app hashes did not commit to Ethereum execution results.</p><p>Migration MUST use:</p><ol><li>a new CometBFT genesis document;</li><li>a new protocol version;</li><li>an explicitly selected Ethereum genesis or checkpoint;</li><li>a genesis execution commitment agreed out of band; and</li><li>clean validator state stores and dedicated EL datadirs.</li></ol><p>Old asynchronous delivery endpoints and persisted delivery statuses SHOULD be removed rather than carried into the consensus state.</p><h2 id="required-test-matrix" tabindex="-1">Required Test Matrix <a class="header-anchor" href="#required-test-matrix" aria-label="Permalink to &quot;Required Test Matrix&quot;">​</a></h2><p>Before this RFC can move to Accepted, automated tests MUST demonstrate:</p><h3 id="determinism" tabindex="-1">Determinism <a class="header-anchor" href="#determinism" aria-label="Permalink to &quot;Determinism&quot;">​</a></h3><ul><li>Four validators independently accept the same payload and app hash.</li><li>Transaction membership and ordering are identical on all ELs.</li><li>All validators derive identical timestamps, randomness, roots, and Engine API versions.</li></ul><h3 id="byzantine-proposals" tabindex="-1">Byzantine proposals <a class="header-anchor" href="#byzantine-proposals" aria-label="Permalink to &quot;Byzantine proposals&quot;">​</a></h3><ul><li>Wrong parent, block hash, state root, transaction root, and receipts root.</li><li>Reordered, removed, inserted, duplicated, and malformed transactions.</li><li>Wrong fork version and fork-specific fields.</li><li>Oversized and non-canonical envelope encodings.</li></ul><h3 id="el-failures" tabindex="-1">EL failures <a class="header-anchor" href="#el-failures" aria-label="Permalink to &quot;EL failures&quot;">​</a></h3><ul><li>EL unavailable during <code>PrepareProposal</code>.</li><li>EL unavailable, <code>SYNCING</code>, <code>ACCEPTED</code>, and timeout during <code>ProcessProposal</code>.</li><li>One validator running an incompatible EL or fork schedule.</li><li>Local EL head changed outside EthBFT.</li></ul><h3 id="crash-recovery" tabindex="-1">Crash recovery <a class="header-anchor" href="#crash-recovery" aria-label="Permalink to &quot;Crash recovery&quot;">​</a></h3><ul><li>Crash before <code>engine_newPayload</code>.</li><li>Crash after validation but before CometBFT decision.</li><li>Crash after the prepared state write.</li><li>Crash after forkchoice update but before application <code>Commit</code>.</li><li>Crash after durable <code>Commit</code> but before the ABCI response.</li><li>Replay of the same decided block at every crash point.</li></ul><h3 id="network-behavior" tabindex="-1">Network behavior <a class="header-anchor" href="#network-behavior" aria-label="Permalink to &quot;Network behavior&quot;">​</a></h3><ul><li>One Byzantine validator among four.</li><li>Proposer failure and proposer rotation.</li><li>Validator partition and recovery.</li><li>New validator synchronization from genesis and from checkpoint.</li></ul><p>The test suite MUST compare committed block hash, state root, receipts root, transaction root, app hash, and canonical EL head across all honest validators.</p><h2 id="implementation-plan" tabindex="-1">Implementation Plan <a class="header-anchor" href="#implementation-plan" aria-label="Permalink to &quot;Implementation Plan&quot;">​</a></h2><h3 id="milestone-1-protocol-types" tabindex="-1">Milestone 1: Protocol types <a class="header-anchor" href="#milestone-1-protocol-types" aria-label="Permalink to &quot;Milestone 1: Protocol types&quot;">​</a></h3><ul><li>Add <code>pkg/protocol</code> with versioned metadata and deterministic hashing.</li><li>Move consensus parameters into a genesis-backed structure.</li><li>Add golden encoding and hash vectors.</li></ul><h3 id="milestone-2-proposal-validation" tabindex="-1">Milestone 2: Proposal validation <a class="header-anchor" href="#milestone-2-proposal-validation" aria-label="Permalink to &quot;Milestone 2: Proposal validation&quot;">​</a></h3><ul><li>Implement the new proposal layout.</li><li>Implement static proposal validation and Engine API version selection.</li><li>Implement strict <code>engine_newPayload</code> validation in <code>ProcessProposal</code>.</li></ul><h3 id="milestone-3-proposal-building" tabindex="-1">Milestone 3: Proposal building <a class="header-anchor" href="#milestone-3-proposal-building" aria-label="Permalink to &quot;Milestone 3: Proposal building&quot;">​</a></h3><ul><li>Move payload production into <code>PrepareProposal</code>.</li><li>Introduce a dedicated builder interface.</li><li>Ensure payload transactions become the exact ordered CometBFT proposal transactions.</li></ul><h3 id="milestone-4-commit-and-recovery" tabindex="-1">Milestone 4: Commit and recovery <a class="header-anchor" href="#milestone-4-commit-and-recovery" aria-label="Permalink to &quot;Milestone 4: Commit and recovery&quot;">​</a></h3><ul><li>Replace the JSON state file with a transactional store.</li><li>Implement prepared and committed records.</li><li>Move canonical forkchoice advancement to decided-block finalization.</li><li>Implement idempotent replay and startup reconciliation.</li></ul><h3 id="milestone-5-multi-validator-e2e" tabindex="-1">Milestone 5: Multi-validator E2E <a class="header-anchor" href="#milestone-5-multi-validator-e2e" aria-label="Permalink to &quot;Milestone 5: Multi-validator E2E&quot;">​</a></h3><ul><li>Run at least four CometBFT validators and four independent EL clients.</li><li>Add Byzantine proposal, crash-window, and divergence tests.</li><li>Add required readiness and consensus metrics.</li></ul><h3 id="milestone-6-state-sync-and-proofs" tabindex="-1">Milestone 6: State sync and proofs <a class="header-anchor" href="#milestone-6-state-sync-and-proofs" aria-label="Permalink to &quot;Milestone 6: State sync and proofs&quot;">​</a></h3><ul><li>Implement paired ABCI/EL checkpoints.</li><li>Implement snapshot restoration validation.</li><li>Add query proofs and light-client-facing execution commitments.</li></ul><h2 id="acceptance-criteria" tabindex="-1">Acceptance Criteria <a class="header-anchor" href="#acceptance-criteria" aria-label="Permalink to &quot;Acceptance Criteria&quot;">​</a></h2><p>This RFC may move from Draft to Accepted only when:</p><ol><li>canonical RLP metadata and protocol-parameter hash vectors are merged;</li><li>consensus-critical genesis parameters are fully specified;</li><li>four-validator E2E tests pass under the required failure matrix;</li><li>crash recovery is proven idempotent at every defined commit phase;</li><li>no local configuration can alter execution consensus;</li><li>blob transactions are either fully specified or explicitly disabled; and</li><li>an independent security review finds no unresolved consensus-safety issue.</li></ol><h2 id="open-questions" tabindex="-1">Open Questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open Questions&quot;">​</a></h2><p>The following questions must be resolved before Accepted status:</p><ol><li>Will the production builder use a dedicated unmodified Geth txpool, a custom Geth extension with an explicit transaction allowlist, or an embedded builder?</li><li>What transactional database and checkpoint format will be used?</li><li>What is the post-MVP Ethereum fork schedule for the chain?</li><li>How will CometBFT validator updates be committed into EthBFT protocol state?</li><li>What proof format will external light clients use to verify execution commitments?</li><li>Will fee revenue be burned, assigned to the proposer, or distributed by a consensus contract?</li></ol><h2 id="security-summary" tabindex="-1">Security Summary <a class="header-anchor" href="#security-summary" aria-label="Permalink to &quot;Security Summary&quot;">​</a></h2><p>EthBFT safety depends on both CometBFT consensus and deterministic Ethereum payload validation. CometBFT prevents two conflicting proposals from being committed at the same height under its standard fault assumption. Independent EL validation prevents a proposer from committing an invalid Ethereum state transition. The execution commitment binds the CometBFT application history to the resulting Ethereum chain.</p><p>This design does not make a single EL implementation bug impossible. Production networks SHOULD consider execution-client diversity after the protocol is stable, but cross-client consensus compatibility must be tested before mixed clients are allowed in the validator set.</p>`,199)])])}const m=a(s,[["render",o]]);export{u as __pageData,m as default};
